# Homebrew Tap for DeepSeekCode

Install DeepSeekCode from the published GitHub Release assets:

```bash
brew tap willamhou/deepseekcode
brew install deepseek
deepseek version
```

The formula installs the `deepseek` CLI from
`willamhou/DeepSeekCode` release archives.
